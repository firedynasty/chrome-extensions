javascript:(function(){if(window.readingRuler){window.readingRuler.cleanup();}else{const sizePresets=[1,2,3,5,8,12,17,23,30,40];let currentSize=parseInt(localStorage.getItem('readingRulerSize')||'0');if(currentSize<0||currentSize>9)currentSize=0;let height=sizePresets[currentSize];const ruler=document.createElement('div');ruler.style.cssText='position:fixed;left:0;right:0;top:0;pointer-events:none;z-index:9999;background:rgba(255,165,0,0.8);box-shadow:0 0 10px rgba(255,165,0,0.5),0 0 20px rgba(255,165,0,0.3);';document.body.appendChild(ruler);let isActive=true;let currentY=0;const topAnchor=80;const step=44;const updateSize=(idx)=>{currentSize=idx;height=sizePresets[idx];ruler.style.height=height+'px';localStorage.setItem('readingRulerSize',idx);};const setY=(y)=>{currentY=Math.max(0,Math.min(window.innerHeight-height,y));ruler.style.top=currentY+'px';};updateSize(currentSize);const handleMouseMove=(e)=>{if(isActive)setY(e.clientY);};const handleKeyPress=(e)=>{switch(e.key){case ' ':isActive=!isActive;e.preventDefault();break;case 'q':case 'Q':isActive=false;setY(topAnchor);break;case 'w':case 'W':isActive=false;if(currentY+step>window.innerHeight-height)setY(topAnchor);else setY(currentY+step);break;}};document.addEventListener('mousemove',handleMouseMove);document.addEventListener('keydown',handleKeyPress);window.readingRuler={setSize:(idx)=>updateSize(idx),getSize:()=>currentSize,cleanup:()=>{document.removeEventListener('mousemove',handleMouseMove);document.removeEventListener('keydown',handleKeyPress);if(ruler.parentNode)document.body.removeChild(ruler);delete window.readingRuler;console.log('Reading ruler removed.');}};console.log('Reading ruler activated! Q=top, W=step, Space=pause, use resize bookmarklet to set size');}})();
